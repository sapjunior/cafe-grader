%h1 
  Sites for
  = @country.name

= flash[:notice]

- form_for :login, nil, :url => {:action => 'site_login'} do |f|
  %p
    Site:
    = select :login, :site_id, @sites.collect {|s| [s.name, s.id]}
  %p
    Password:
    = f.password_field :password
  %p= submit_tag 'Login'
