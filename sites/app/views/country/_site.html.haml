
%tr.site
  %td= site_counter + 1
  %td=h site.name
  %td=h site.password
  %td
    = link_to '[edit]', :action => 'edit', :id => site.id
    = link_to '[delete]', :action => 'delete', :id => site.id, :confirm => "Are you sure?"
